# -----------------------------
# 📂 Path Configurations
# -----------------------------
# ตำแหน่งโฟลเดอร์เก็บ reference และ stats
DATA_DIR=./references

# -----------------------------
# ⚙️ Logging Configuration
# -----------------------------
# DEBUG = ระหว่างพัฒนา
# INFO  = แนะนำสำหรับ production
LOG_LEVEL=INFO

# -----------------------------
# 🌐 API Settings
# -----------------------------
# พอร์ต API
API_PORT=8000

# ระบุ origin ของ frontend
# dev ใช้ *, production ระบุโดเมนจริง
# ตัวอย่าง production:
# CORS_ALLOW_ORIGINS=https://pose-coach.example.com
CORS_ALLOW_ORIGINS=*

# -----------------------------
# 🔧 Model / Processing Settings
# -----------------------------
# จำนวน keypoints ที่โมเดลใช้ (MoveNet = 17)
NUM_KEYPOINTS=17

# FPS เป้าหมายสำหรับสกัด keypoints จากวิดีโอ (ไม่ได้ใช้แล้ว แต่เก็บเผื่อ)
TARGET_FPS=30

# -----------------------------
# 🧪 Testing Mode
# -----------------------------
# false = ใช้ระบบจริง (read-only data provider)
# true  = mock data สำหรับ unit test
ENABLE_TEST_MODE=false
